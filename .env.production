# Production environment template
# Copy this to .env in production and update with your values
# NEVER commit .env files with actual production values to version control

# Application environment
APP_ENV=production

# Server configuration
APP_SERVER__HOST=0.0.0.0
APP_SERVER__PORT=8080

# Database configuration
# Use strong credentials and SSL/TLS connection in production
APP_DATABASE__URL=postgres://username:password@production-db-host:5432/rusty_saas
APP_DATABASE__MAX_CONNECTIONS=100
APP_DATABASE__MIN_CONNECTIONS=10

# JWT configuration
# CRITICAL: Generate a strong random secret with: openssl rand -hex 64
# Rotate this secret regularly in production
APP_JWT__SECRET=CHANGE_THIS_TO_A_STRONG_RANDOM_VALUE_MINIMUM_64_CHARACTERS
APP_JWT__EXPIRATION_HOURS=24

# CORS configuration
# Update with your actual production domains
APP_CORS__ALLOWED_ORIGINS=https://yourdomain.com,https://api.yourdomain.com

# Logging
# Use 'info' level in production to reduce log volume
# Use 'warn' or 'error' for even less verbosity
RUST_LOG=rusty_saas=info,tower_http=info

# Optional: Sentry error tracking
# SENTRY_DSN=your-sentry-dsn-here

# Optional: Application insights / telemetry
# TELEMETRY_ENDPOINT=your-telemetry-endpoint
